packages:
  #Include base device definition
  minimal: !include common.yaml

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

web_server:
  version: 3

ota:
  - platform: http_request
  - platform: esphome
    allow_partition_access: true

http_request:
  verify_ssl: false
